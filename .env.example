# Copie este arquivo para ".env" e preencha os valores.
# O ".env" está no .gitignore e NÃO vai para o repositório.

# --- Banco Oracle do Sankhya (mesmas credenciais da internal-api-sankhya) ---
DB_USER=usuario
DB_PASS=senha
DB_DSN=192.168.255.250:1521/xe

# --- Token da API comercial do cnpj.ws ---
CNPJWS_TOKEN=DFNBJFE3NR5mYw2ze92CESzPRMobLsSRdFEviRC6kMox

# --- Opcionais ---
# Caminho do banco SQLite onde os dados enriquecidos ficam salvos
DB_SQLITE=enriquecimento.db
# Pausa (em segundos) entre cada chamada na API, para respeitar o rate limit
CNPJWS_DELAY=0.3
